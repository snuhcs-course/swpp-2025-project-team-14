<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- 테두리 설정 -->
    <stroke
        android:width="1dp"
        android:color="@color/text_secondary" /> <!-- 조금 더 연한 회색으로 설정 -->
    <!-- 내부 색상: 투명 -->
    <solid android:color="@android:color/transparent" />
</shape>
