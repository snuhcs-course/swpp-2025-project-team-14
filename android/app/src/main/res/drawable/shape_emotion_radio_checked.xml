<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- 테두리가 필요하다면 stroke를 유지하고, 필요 없다면 이 부분을 삭제해도 됩니다. -->
    <stroke
        android:width="1dp"
        android:color="@color/text_secondary" /> <!-- 테두리 색상 -->
    <!-- 내부 색상: text_secondary 색상으로 채웁니다. -->    <solid android:color="@color/text_secondary" />
</shape>
