<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardTutorial"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintDimensionRatio="9:19"
        app:cardCornerRadius="40dp"
        app:strokeWidth="3dp"
        app:strokeColor="@android:color/black"
        app:cardUseCompatPadding="false"
        app:cardPreventCornerOverlap="true">

        <ImageView
            android:id="@+id/ivTutorial"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitXY"
            android:contentDescription="@null" />
    </com.google.android.material.card.MaterialCardView>

    <!-- 2) 투명 프레임 오버레이 -->
    <ImageView
        android:id="@+id/ivFrame"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="@id/cardTutorial"
        app:layout_constraintBottom_toBottomOf="@id/cardTutorial"
        app:layout_constraintStart_toStartOf="@id/cardTutorial"
        app:layout_constraintEnd_toEndOf="@id/cardTutorial"
        android:src="@drawable/tutorial_outline"
        android:scaleType="fitXY"
        android:adjustViewBounds="false"
        android:clickable="false"
        android:focusable="false"
        android:contentDescription="@null"/>

</androidx.constraintlayout.widget.ConstraintLayout>